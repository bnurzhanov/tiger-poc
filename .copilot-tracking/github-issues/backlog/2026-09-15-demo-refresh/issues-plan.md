<!-- markdownlint-disable-file -->
<!-- markdown-table-prettify-ignore-start -->
---
title: Approved GitHub Issue Operations
description: Source of truth for the two-milestone backlog refresh and exact GitHub-bound payloads.
---

## Plan

Follow github-backlog-planning.instructions.md for issue field conventions and content sanitization. Repository: dkirby-ms/tiger-poc. All existing referenced issues are open. Existing labels, assignees, comments, and state remain unchanged. Milestone keys and operation keys below are local routing metadata, not GitHub-bound content. Resolve new issue dependency references after creation.

```json
{
  "repository": "dkirby-ms/tiger-poc",
  "resolvedMilestones": {"M1": 1, "M2": 2},
  "resolvedIssues": {"model-validation": 22, "demo-viewer": 23, "fabric-walkthrough": 24},
  "dependencyLinks": {
    "12": "Physical model suitability is tracked by #22; the recording view consuming this adapter is #23.",
    "14": "Use the two-camera model evidence from #22. Confirmed occupancy and availability feed the view in #23.",
    "17": "The integrated physical-to-twin acceptance walkthrough is #24; mapping validation alone does not replace it.",
    "18": "The integrated physical-to-twin acceptance walkthrough is #24, following representation work in #17.",
    "19": "Requires physical model validation #22 and the side-by-side viewer #23. Completion is the local-demo prerequisite for the separate Fabric walkthrough #24."
  },
  "milestones": [
    {
      "key": "M1",
      "title": "M1: Local Two-Cell Demo",
      "description": "Two physical RTSP cameras, two manifests, and two concurrent instances on one or more local developer machines. Real validated pallet inference, region overlays, confirmed independent occupancy transitions, separate local ProcessEvent outputs, and a two-minute partner recording. No Foundry Local, Azure Local, Foundry cloud inference, Fabric, or inventory reconciliation is required for this milestone."
    },
    {
      "key": "M2",
      "title": "M2: Fabric Digital Twin Representation",
      "description": "Represent both physical cells, their plants, pallet positions, and observations in Microsoft Fabric. Publish the existing locally produced ProcessEvent contract through the verified Eventstream landing and Digital twin builder mapping path. Show correct independent position updates, relationships, last confirmed occupancy, observation timestamps, and measured event-to-view latency. Inference remains on local developer machines; Foundry and Azure Local are not required."
    }
  ],
  "issues": [
    {
      "action": "create",
      "key": "model-validation",
      "milestone": "M1",
      "labels": ["inference", "testing", "priority:high"],
      "title": "Validate pallet detection on both physical camera setups",
      "body": "## Summary\n\nMilestone 1: validate an existing pallet-capable model on both physical RTSP camera viewpoints before relying on it for presence decisions. Run inference on ordinary local developer machines; assume Foundry Local and Azure Local are unavailable. This is model suitability evidence, not a provider-comparison or model-training task.\n\n## Acceptance criteria\n\n- Record occupied and empty observations from both actual camera viewpoints, including relevant lighting and occlusion limitations.\n- Verify the model supports the pallet class and produces usable detection geometry; generic object-detector availability alone does not satisfy this requirement.\n- Record the selected model/version, local runtime, configuration, observed detection errors, and conditions under which evidence must be considered unusable.\n- Establish practical confidence and region-matching inputs for #14; distinguish detector confidence from confidence in absence.\n- Run both workloads concurrently on the selected dev-box placement and measure frame freshness, dropped frames, throughput, and inference latency. Record hardware and placement.\n- Preserve enough physical evidence to justify the demo claims without exposing camera endpoints or credentials. If no existing model is suitable, record the blocker and revised options rather than substituting mock inference.\n\n## Dependencies and scope\n\nUse the scenario and position definitions from #10. The current local detector can be used for initial assessment; reusable adapter work is tracked by #12. This work gates real-camera presence validation and the recording in #19. Training, Foundry deployment, and arbitrary-viewpoint generalization are out of scope.\n\n## References\n\n- `docs/mvp-design.md`\n- `docs/demo-script.md`\n- `apps/detect/rtsp_yolo.py`"
    },
    {
      "action": "create",
      "key": "demo-viewer",
      "milestone": "M1",
      "labels": ["enhancement", "priority:high"],
      "title": "Build the side-by-side two-cell demo view",
      "body": "## Summary\n\nMilestone 1: provide the minimal recording view for two independently running camera workloads. Show actual feeds, manifest-defined regions, confirmed occupancy, evidence availability, and latest local events together. Support the selected deployment on one or more local developer machines without a new cloud dependency.\n\n## Acceptance criteria\n\n- Both physical camera feeds remain visible side by side with stable source and pallet-position identity.\n- Region overlays use each manifest's geometry and coordinate convention; the view does not hard-code one shared camera angle.\n- Display unknown, confirmation-pending, occupied, empty, and unavailable/stale evidence clearly without treating a failed frame as empty.\n- Keep last confirmed occupancy distinct from evidence availability and show event timestamps and the latest ProcessEvent for each instance.\n- Feed, state, and event updates stay associated with their own source/subject; simultaneous changes do not overwrite the other cell.\n- Camera credentials, connection endpoints, and secrets never appear in the recording view or visible errors.\n- Document how to start or connect the view to the chosen local instances. Verify concurrent operation, camera disconnect/recovery, and an unchanged state without repeated transition events.\n\n## Dependencies and scope\n\nConsumes configured identities and regions from #10, capture/inference outputs from #12, confirmed state from #14, and local events from #15. The view supports rehearsal in #19. A polished end-user application, Fabric visualization, and inventory reconciliation are out of scope.\n\n## References\n\n- `docs/demo-script.md`\n- `docs/mvp-design.md`"
    },
    {
      "action": "create",
      "key": "fabric-walkthrough",
      "milestone": "M2",
      "labels": ["fabric", "digital-twin", "testing", "priority:high"],
      "title": "Validate the two-cell Fabric digital twin end to end",
      "body": "## Summary\n\nMilestone 2: demonstrate the two physical cells and their independent pallet-position observations in Fabric using the unchanged local inference path. This is the complete MVP evidence gate, after the local demo and Fabric landing, mapping, and sink work.\n\n## Acceptance criteria\n\n- Inspect the actual Plant, Cell, PalletPosition, and Observation representation and relationships for both setups in Fabric.\n- Run both real-camera workloads on local developer machines with Fabric publication configured, without Foundry or Azure Local dependencies or changes to inference/presence-rule logic.\n- Place a pallet in Cell A, then Cell B, then remove it from Cell A; trace each confirmed local event through landing and mapping to only its corresponding position update.\n- Verify source, subject, plant, and configured cell attribution, boolean PalletPresent values, event IDs, and observation timestamps.\n- Show last confirmed values and timestamps honestly. Camera loss must not cause an empty update; silence between transition events must not be presented as proof of live camera health.\n- Measure event-to-view latency and document the observed ingestion/mapping refresh behavior, with labeled time cuts where needed. Do not assume immediate updates or a five-minute target.\n- Record failures and blockers with evidence, impact, owner, and next action. Missing Fabric access or a portal-only mapping mock does not satisfy the end-to-end acceptance criteria.\n- Document a repeatable walkthrough and retain redacted physical-to-twin evidence. Recorded payloads remain useful for connector tests but do not replace this demonstration.\n\n## Dependencies\n\nRequires local demo validation #19, verified landing #16, twin representation/view #17, and Fabric sink #18. This walkthrough is separate from the two-minute local-only recording.\n\n## References\n\n- `docs/mvp-design.md`\n- `docs/demo-script.md`"
    },
    {
      "action": "update",
      "number": 10,
      "milestone": "M1",
      "title": "Configure two pallet-presence cells with independent manifests",
      "body": "## Summary\n\nMilestone 1: the selected scenario is pallet presence at one monitored position per physical camera. Make the same application runnable twice from separate PerceptionWorkload manifests on one or more local developer machines. The scenario is decided; implementation must align the currently conflicting manifest and runtime configuration with it.\n\n## Acceptance criteria\n\n- Two prepared manifests select distinct physical RTSP camera references, region geometry, sourceId, subjectId, and local output paths. Each includes plantName and plantId; plant metadata may be shared when both cells belong to the same plant.\n- Configuration defines an explicit region coordinate convention, matching criterion, local model/provider selection, confidence settings, occupied/empty confirmation windows, and stale-evidence timeout.\n- The application validates and loads the selected manifest, fails clearly for invalid geometry or missing identity/settings, and starts one instance per manifest without application-code changes.\n- Launch commands support concurrent operation on ordinary dev boxes and document camera connectivity and sufficient compute. Foundry Local, Azure Local, and Foundry cloud inference are not prerequisites.\n- The first event is ProcessEvent with observationType PalletPresent, boolean value, stable position identity, and documented units, capture/produce/publish times, and presence/absence confidence semantics.\n- Camera credentials and connection endpoints are resolved outside the displayed manifests and excluded from public events, logs, and recording.\n- Root manifest, setup documentation, MVP design, and demo script consistently describe M1 local outputs and M2 Fabric digital twin representation. Inventory/WMS reconciliation and provider-comparison gates are not MVP scope.\n\n## Dependencies and scope\n\nCoordinate the existing shared contract with #11, capture/inference wiring with #12, and presence policy with #14. Future Fabric configuration is implemented in #16-#18; it must not block the local demo.\n\n## References\n\n- `manifest.yaml`\n- `docs/mvp-design.md`\n- `docs/demo-script.md`\n- `apps/detect/README.md`"
    },
    {
      "action": "update",
      "number": 11,
      "milestone": "M1",
      "title": "Verify versioned contracts for pallet presence and evidence availability",
      "body": "## Summary\n\nMilestone 1: reuse the existing versioned Frame, RawDetection, RawInference, Observation, ProcessEvent, and Sink work. Verify and fill any gaps needed for two-camera pallet presence rather than replacing the implemented contract.\n\n## Acceptance criteria\n\n- The shared definitions distinguish frame capture, inference production, and event publication timestamps and retain source/provider/model identity.\n- Successful inference with zero detections is distinguishable from failed, stale, or unavailable evidence. Detection geometry and its coordinate convention support configured region matching.\n- Confirmed presence events use eventType ProcessEvent, observationType PalletPresent, boolean values, stable sourceId/subjectId, schema version, and repeatable event identity for retries.\n- Preserve plantName/plantId attribution using the existing schema's supported extension mechanism; define how unresolved subjects and availability remain distinct from a confirmed false value. Do not silently introduce a breaking schema.\n- Document confidence semantics for both presence and absence; a detector score is not automatically an absence score.\n- Existing and additional fixtures/tests reject missing IDs, invalid timestamps, unsupported types, and invalid confidence, and cover true/false values, both source identities, plant metadata, and unusable evidence.\n- Raw detections remain available through the established normalized boundary for troubleshooting and region processing without making provider-specific output the downstream contract.\n\n## Existing work and dependencies\n\nContract and schema files already exist locally. Review their tests and retain completed work; this issue stays open until the refined criteria are verified. Scenario identity and policy are coordinated with #10 and #14.\n\n## References\n\n- `apps/detect/tiger_perception/contracts.py`\n- `apps/detect/tiger_perception/schemas/process-event-v1.json`\n- `apps/detect/tests/test_contracts.py`\n- `docs/mvp-design.md`"
    },
    {
      "action": "update",
      "number": 12,
      "milestone": "M1",
      "title": "Extract physical-camera capture and local inference adapters",
      "body": "## Summary\n\nMilestone 1: separate capture and inference from the current detector while retaining a working real RTSP/local-model path. Each instance consumes its manifest and owns its state. Replay and fake inference remain development/test tools, not substitutes for the physical demo.\n\n## Acceptance criteria\n\n- Physical RTSP and prerecorded replay produce the shared Frame format with source identity and capture timestamps.\n- The existing local model path emits normalized detections, geometry, provider/model provenance, and explicit inference success/failure usable by #14.\n- Failed reads, stale frames, and inference errors cannot masquerade as successful frames with no detections. Disconnect and recovery behavior is testable and documented.\n- Two manifest-selected camera instances run concurrently on the chosen local dev boxes without shared occupancy state or hard-coded source identity.\n- Capture/inference buffering is bounded, with frame drops, processing delay, and evidence freshness observable.\n- The existing rtsp_yolo.py command remains usable as a compatibility entry point.\n- Deterministic replay and fake-provider tests run without cameras or cloud credentials; physical inference remains the required demo path.\n- No Foundry Local, Azure Local, or cloud provider implementation is required.\n\n## Dependencies\n\nUses #10 configuration and #11 contracts. Real model suitability is validated separately before recording; generic adapter extraction does not prove pallet recognition.\n\n## References\n\n- `apps/detect/rtsp_yolo.py`\n- `apps/detect/tiger_perception/replay.py`\n- `docs/mvp-design.md`"
    },
    {
      "action": "update",
      "number": 13,
      "milestone": null,
      "title": "[Post-MVP] Compare inference providers using the same replay",
      "body": "## Scope status\n\nDeferred beyond both MVP milestones. Neither the two-cell local demo nor Fabric digital twin representation requires provider comparisons. Keep this issue open as optional follow-on work, with no MVP milestone or dependency.\n\n## Summary\n\nWhen provider comparison is prioritized, run identical replay input through the deterministic test provider and a validated real inference adapter. Compare normalized observations and resulting events while keeping mapper and sink implementations unchanged.\n\n## Future acceptance criteria\n\n- Each provider produces schema-valid events and preserves source/provider/model provenance.\n- Equivalent confirmed transitions preserve the agreed event identity policy; do not require equal outputs or IDs for semantically different detections.\n- Describe differences in detection quality, confirmation timing, and event values rather than interpreting a mock comparison as physical-model validation.\n- Capture a repeatable command or automated test and document findings in an inference-provider readiness report.\n\n## Relationship to the MVP\n\nM1 uses a validated local model on both physical cameras. M2 keeps that inference path and adds Fabric representation. Foundry expansion is tracked separately in #20.\n\n## References\n\n- `docs/mvp-design.md`"
    },
    {
      "action": "update",
      "number": 14,
      "milestone": "M1",
      "title": "Confirm region-based pallet presence and emit isolated transitions",
      "body": "## Summary\n\nMilestone 1: map normalized detections to each manifest's pallet-position region and maintain confirmed occupancy independently per sourceId and subjectId. Emit the initial confirmed boolean state, then only confirmed transitions.\n\n## Acceptance criteria\n\n- Region geometry, matching criterion, confidence policy, and occupied/empty confirmation windows come from configuration.\n- Start unknown; emit a PalletPresent boolean only after usable evidence confirms occupied or empty.\n- Confirm occupied from qualifying pallet detections. Confirm empty only from usable frames with successful inference and no qualifying pallet for the configured window.\n- Unchanged observations do not produce repeated transition events; independent changes in Cell A and Cell B never affect each other's state.\n- Failed, stale, missing, low-quality, or otherwise unusable evidence cannot count toward empty confirmation. Reset pending confirmation and expose unavailable status separately from last confirmed occupancy.\n- Recovery requires fresh confirmation; recovery to the unchanged last confirmed value creates no extra transition event.\n- Retries of the same event preserve identity. Define out-of-order input handling and do not let stale observations overwrite current confirmation.\n- A restart begins a new initialization sequence for M1; persistent state across restarts is not required.\n- Tests cover region boundaries, brief flicker, initial true/false confirmation, transitions, unchanged values, two-source isolation, confidence, stale/failed evidence, disconnect/recovery, ordering, restart, and retries.\n- Presence logic and ProcessEvent mapping remain independent of Fabric transport and provider-specific payloads.\n\n## Dependencies\n\nRequires #10 policy/configuration, #11 contracts, #12 normalized evidence, and model suitability evidence from both physical views.\n\n## References\n\n- `docs/mvp-design.md`\n- `apps/detect/tiger_perception/contracts.py`"
    },
    {
      "action": "update",
      "number": 15,
      "milestone": "M1",
      "title": "Verify separate schema-validating local outputs for both cells",
      "body": "## Summary\n\nMilestone 1: reuse the existing local JSONL sink as the required demo destination, not merely an offline fallback. Connect each manifest-driven instance to its own output while preserving validation, redaction, safe flushing, and observable failure behavior.\n\n## Acceptance criteria\n\n- Two concurrent instances write separate manifest-selected local outputs containing schema-valid ProcessEvent records from real confirmed pallet-presence transitions.\n- Each record preserves sourceId, subjectId, plant metadata, boolean PalletPresent value, event ID, and timestamps without cross-cell mixing.\n- Invalid events fail before publication, and credentials/connection endpoints are absent from public payloads, logs, and exception text.\n- Safe flush/shutdown and outage/recovery tests demonstrate bounded retry behavior and visible failures without silent loss. Duplicate handling and retry identity are deterministic and documented.\n- Publication counters and latest-event access support diagnostics and the demo view without coupling the sink to UI behavior.\n- Existing local sink and replay tests remain passing; add only coverage needed for metadata and concurrent-output isolation.\n- Setup documentation distinguishes the physical two-instance launch/output workflow from offline replay, which remains useful for testing.\n\n## Existing work and dependencies\n\nThe local sink and tests already exist. Retain that implementation and verify the refined criteria before closing. Uses manifest paths from #10, contract #11, and confirmed events from #14. Fabric publication is separate in #18.\n\n## References\n\n- `apps/detect/tiger_perception/sinks.py`\n- `apps/detect/tests/test_local_jsonl_sink.py`\n- `apps/detect/README.md`\n- `docs/demo-script.md`"
    },
    {
      "action": "update",
      "number": 16,
      "milestone": "M2",
      "title": "Verify dev-box events land in Fabric for digital twin mapping",
      "body": "## Summary\n\nMilestone 2: test one Eventstream custom-app protocol and the actual downstream landing destination needed for Digital twin builder. Publish schema-valid events from a local developer machine; no Foundry or Azure Local runtime is involved. Treat the Eventstream-to-twin arrow in the design as a logical path whose physical landing and refresh steps must be verified.\n\n## Acceptance criteria\n\n- Publish a small repeatable batch, including both cells and occupied/empty values, and inspect the real destination table/storage.\n- Confirm the protocol, destination, schema, identity fields, plant metadata, boolean types, and timestamp handling needed by #17.\n- Record authentication/secret-reference method, dev-box network reachability, TLS requirements, ingestion delay, retries, and failure behavior without exposing secrets.\n- Document the measured Eventstream landing-to-Digital twin builder path and any tenant/capacity/preview constraints; do not assume direct or instantaneous ontology updates.\n- Capture a repeatable publisher or portal procedure and redacted evidence in a Fabric landing report.\n- If blocked, record the exact blocker, owner, impact, and next step; a blocked spike does not count as completed M2 evidence.\n- Sink implementation in #18 uses only the confirmed transport and destination.\n\n## Prerequisites\n\nFabric tenant/capacity, Eventstream access, and Digital twin builder availability. Use the shared contract from #11 and source/subject identities from #10. This spike can be investigated early but does not block the local-only recording.\n\n## References\n\n- `docs/mvp-design.md`\n- `apps/detect/tiger_perception/schemas/process-event-v1.json`"
    },
    {
      "action": "update",
      "number": 17,
      "milestone": "M2",
      "title": "Represent both cells and pallet-position states in Fabric",
      "body": "## Summary\n\nMilestone 2: extend the existing Digital twin builder mapping/refresh work into the small, inspectable digital twin representation required by the updated design. Use the verified landing path to represent Plant, Cell, PalletPosition, and Observation entities for both physical setups.\n\n## Acceptance criteria\n\n- Create stable plant, cell, and pallet-position entities with explicit plant-to-cell and cell-to-position relationships and observations associated with their positions.\n- Map ProcessEvent subjectId to the correct position through configured identity, retain sourceId as camera provenance, and preserve plant attribution. Do not infer keys from display names.\n- Both cells use one ontology and mapping logic with different configured identities; the same plant may contain both cells.\n- Map boolean PalletPresent observations to last confirmed occupancy and retain the relevant observation/event timestamps and traceable event identity.\n- Provide an inspectable Fabric view showing both cells, their relationships, and last confirmed position values with timestamps.\n- Clearly label last confirmed state; silence in transition-only events must not be interpreted as empty or as proof of live camera health. A live availability indicator would require a separate health signal.\n- Verify updates for both positions independently, measure supported mapping/refresh behavior, and record fixed identifier/property/type decisions and platform constraints.\n- Replace the old assumed five-minute target with measured refresh and event-to-view timing. Document the chosen representation and repeatable setup.\n- Missing platform access or an unverified mock representation is a blocker, not fulfillment of the acceptance criteria.\n\n## Dependencies and scope\n\nRequires the actual landing/schema results from #16 and identities from #10. The complete real-camera walkthrough follows #18 and local demo validation #19. Inventory reconciliation, individual pallet identity, and Foundry deployment remain out of scope.\n\n## References\n\n- `docs/mvp-design.md`"
    },
    {
      "action": "update",
      "number": 18,
      "milestone": "M2",
      "title": "Publish local process events through the verified Fabric sink",
      "body": "## Summary\n\nMilestone 2: implement only the transport and destination verified by the Fabric landing spike. Publish events from both local developer-machine workloads while keeping inference, presence rules, and ProcessEvent independent of Fabric. Preserve the existing local JSONL output path for diagnostics/replay.\n\n## Acceptance criteria\n\n- Selecting Fabric publication requires no changes to the validated local inference path, presence-rule implementation, or ProcessEvent schema. No Foundry or Azure Local deployment is required.\n- Both instances publish through the same connector logic with correct source, subject, plant, event identity, and timestamp attribution.\n- Enforce TLS verification and secret references; credentials and connection endpoints are absent from displayed manifests, public payloads, logs, and exception text.\n- Temporary failures retry with bounded backoff; permanent failures remain visible and are not silently discarded.\n- Retry identity and duplicate protection are tested with captured transport responses. Document delivery guarantees and downstream duplicate handling; do not claim exactly-once delivery without evidence.\n- Structured redacted logging and metrics expose publication outcomes and failures. Local replay/output remains available without Fabric access.\n- Tests cover contract validation, both identities, retries, duplicate protection, TLS behavior, and redaction.\n\n## Dependencies\n\nImplement after #16 confirms the transport and destination. Coordinate entity mappings with #17 and reuse #11 contracts and #15 sink conventions. These dependencies concern Fabric publication, not the M1 local recording.\n\n## References\n\n- `docs/mvp-design.md`\n- `apps/detect/tiger_perception/sinks.py`"
    },
    {
      "action": "update",
      "number": 19,
      "milestone": "M1",
      "title": "Rehearse and record the two-cell local pallet-presence demo",
      "body": "## Summary\n\nMilestone 1 acceptance: run the two physical camera workloads concurrently on one or more local dev boxes and record the two-minute partner script. Real-camera validation is required, not an optional smoke test after replay. Neither Fabric nor Foundry/Azure Local is needed for this milestone.\n\n## Acceptance criteria\n\n- Contract, adapter, presence-rule, sink, and failure tests pass, along with the project's Ruff and compilation checks.\n- A new team member can prepare and launch the two manifests from documentation without changing application code.\n- Both actual feeds and manifest region overlays are shown side by side, initially confirmed empty, with source/subject identity, evidence availability, latest states, and local events.\n- Place a pallet in Cell A while B remains empty, then in B, then remove A; capture independent confirmed transitions with correct boolean PalletPresent, source, subject, plant, event ID, and timestamp fields.\n- Verify repeated unchanged observations create no repeated transition events and each instance writes to its own local output.\n- Exercise brief flicker, stale/failed inference, camera disconnect, and recovery. Missing/unusable evidence is unavailable, never confirmed empty.\n- Record concurrent compute behavior, frame freshness, dropped frames, inference/confirmation delays, and sink outcomes.\n- Keep camera credentials and endpoints off screen. Retain confirmation delays honestly and label time cuts. Two minutes is video length, not installation/startup time; remove the previous under-five-minute replay acceptance target.\n- Retain redacted evidence and document blockers with owner, impact, and next action. A mock or prerecorded-only demonstration does not satisfy M1.\n\n## Dependencies\n\nRequires #10-#12, #14, #15, physical-model validation, and the side-by-side demo view. Provider comparison #13 and readiness #20 are explicitly not prerequisites.\n\n## References\n\n- `docs/demo-script.md`\n- `docs/mvp-design.md`\n- `apps/detect/README.md`"
    },
    {
      "action": "update",
      "number": 20,
      "milestone": null,
      "title": "[Post-MVP] Assess Foundry and Azure Local provider readiness",
      "body": "## Scope status\n\nOptional post-MVP expansion, outside both milestones. The demo and Fabric digital twin representation run inference on ordinary local developer machines. Assume Foundry Local and Azure Local are unavailable; their access or readiness must not gate M1 or M2.\n\n## Summary\n\nWhen separately prioritized and access is available, assess Foundry cloud inference and Foundry Local on Azure Local behind the existing provider boundary. Preserve the local workload and established Fabric event contract.\n\n## Future acceptance criteria\n\n- Record current access, supported deployment/runtime, hardware, model, identity, TLS, networking, and platform prerequisites from verified evidence.\n- Validate a pallet-capable model's supported input/output format and detection geometry before claiming compatibility with region presence rules.\n- Measure startup, p50/p95 latency, throughput, output quality, and confirmation behavior where access exists; otherwise name the blockers without inventing results.\n- Produce schema-valid events and document any semantic differences without changing the Fabric connector solely for provider selection.\n- Document data egress and connected/disconnected operating assumptions.\n- Record a clear go/no-go decision in a provider-readiness report. Local inference success is not evidence of Foundry or Azure Local compatibility.\n\n## Relationships\n\nProvider comparisons may reuse #13 after this expansion is prioritized. Neither issue belongs to an MVP milestone or blocks the two-cell Fabric walkthrough.\n\n## References\n\n- `docs/mvp-design.md`"
    }
  ]
}
```

## Execution Order And Relationships

Create the two milestones, then the three new issues, then update #10 through #20 sequentially. Append each matching dependencyLinks value to the corresponding update body under a Related delivery issues heading. Resolved milestone and issue maps record verified remote identifiers. No sub-issue hierarchy, closure, reassignment, or label taxonomy change is needed.

The new model-validation issue is distinct from #12 adapter work and #13 provider comparison. The viewer issue implements the tool that #19 consumes. The Fabric walkthrough validates the integrated result after #16 landing, #17 representation, and #18 sink implementation.

<!-- markdown-table-prettify-ignore-end -->